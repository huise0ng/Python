# // Bssm 클래스를 만든다

# // 생성자 블럭으로 변수를 초기화시킨다.

# // intro 라는 메서드를 

class Bssm:
        def _init_(self, task, age, name):
            self.team="부소마"
            self.task=task
            self.age-age
            self.name=name
            
        def intro(self):
                print("hello, %ㄴdptj %s를 담당하고 있는 %d살 %s입니다. " %(self.team, self.task, self.age, self.name))